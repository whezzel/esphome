text_sensor:
  - platform: template
    name: ${room} ${device} Uptime
    id: uptime_human
    icon: mdi:clock-start

  - platform: version
    name: ${room} ${device} ESPHome Version
    id: esphome_version
    hide_timestamp: false
